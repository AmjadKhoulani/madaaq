<IfModule mod_rewrite.c>
  RewriteEngine On

  # Route API requests to PHP Bridge
  RewriteRule ^api/(.*)$ api/index.php [L]

  RewriteBase /
  RewriteRule ^index\.html$ - [L]
  RewriteCond %{REQUEST_URI} !^/api [NC]
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteRule . /index.html [L]
</IfModule>
