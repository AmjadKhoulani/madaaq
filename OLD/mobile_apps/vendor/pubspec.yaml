name: vendor_app
description: Vendor Admin App for Mikrotik SaaS
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.0.0
  shared_preferences: ^2.0.0
  provider: ^6.0.0
  google_fonts: ^6.0.0
  intl: ^0.18.0
  fl_chart: ^0.66.0    # For Dashboard Charts
  webview_flutter: ^4.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
